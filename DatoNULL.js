/*
Este tipo de dato es usado para representar la ausencia de un valor, básicamente decir que no contiene nada,
esta vacía o que todavía no se conoce su valor. Es un tipo de dato a parte y solo tiene un valor: "NULL".
Es utilizada con frecuencia para inicializar variables que después muy posiblemente si resiban un valor.
*/

//Ejemplo de uso y aplicación del valor NULL
let null_valorexample = null;
console.log(null_valorexample);
null_valorexample = 10 + 5;
console.log(null_valorexample);

